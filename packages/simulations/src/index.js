/**
 * @newton/simulations entry point
 *
 * Simulation Registry. Reusable, trusted React simulation components. AI
 * selects an id + params; never generates rendering logic.
 */
export { registry, default } from './registry.js';
