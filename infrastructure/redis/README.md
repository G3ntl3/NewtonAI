# Redis (Phase 2)

Planned usage:
- Conversation context cache (MemoryManager)
- Token-budget/session cache (CostOptimizer)

Not wired in Phase 1 — interface should already anticipate this (see ADR 0001/blueprint §8).
